spring.application.name=web
api.prefix=/api/v1

# JDBC URL with SSL mode
spring.datasource.url=jdbc:postgresql://hopper.proxy.rlwy.net:50936/railway
spring.datasource.username=postgres
spring.datasource.password=HoMToLoYzkXmGmjclKaSiyzvNBphelGb
spring.datasource.driver-class-name=org.postgresql.Driver

# Optional Hibernate config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true


# HikariCP tuning (only once)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.leak-detection-threshold=5000
logging.level.org.springframework.jdbc=DEBUG
logging.level.org.hibernate.SQL=DEBUG


# JPA / Hibernate

# Port and file upload
server.port=8080
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
